const data = {
  type: 'div',
  children: [{
    type: 'Dialog',
    bind: 'test',
    children: [{
      type: 'Box',
      bind: 'box',
      children: [{
        type: 'Button',
        bind: 'button',
      }],
    }],
  }],
}

module.exports = data

